<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bcp.battery.designpatterns">

    <application
        android:name="bcp.battery.designpatterns.BCPDesignPatternsApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Bcp Desgin Patterns"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name="bcp.battery.designpatterns.BCPDesignPatternsActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:label="Bcp Battery Design Patterns"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>